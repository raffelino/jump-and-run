/**
 * Welt 4 - Eisige Berge
 * Schnee und Eis, rutschige Plattformen
 */

import { generateWorldLevels } from '../ProceduralLevelGenerator.js';

// Generiere 5 Level prozedural mit Seed 4000
export const world4 = generateWorldLevels(4000, "Eisige Berge", 5);
